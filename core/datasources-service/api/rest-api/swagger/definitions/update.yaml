type: object
properties:
    name:
        type: string
    versionDescription:
        type: string
    files:
        type: object
        properties:
            mapping:
                type: array
                items:
                    $ref: '#/components/schemas/FileMeta'
            added:
                $ref: '#/components/schemas/Files'
            dropped:
                type: array
                items:
                    type: string
