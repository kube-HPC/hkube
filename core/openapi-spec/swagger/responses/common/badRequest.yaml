description: Bad request
content:
  application/json:
    schema:
      $ref: "#/components/schemas/errorBadRequest"
    example:
      error:
        code: 400
        message: "Unexpected token @ in JSON at position 14"
