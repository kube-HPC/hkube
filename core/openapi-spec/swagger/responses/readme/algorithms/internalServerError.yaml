description: Internal server error when reading algorithm readme
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: integer
              example: 500
            message:
              type: string
              example: "Cannot read properties of undefined (reading 'buffer')"
