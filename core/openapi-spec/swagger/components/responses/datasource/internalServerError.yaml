description: Internal server error due to invalid regular expression
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: integer
    example:
      error:
        code: 500
