type: object
properties:
  name:
    type: string
    default: ""
  experimentName:
    $ref: "#/components/schemas/experimentName"
    default: main
  sort:
    type: string
    default: desc
    enum:
      - asc
      - desc
  limit:
    type: integer
    default: 1
    minimum: 1
    maximum: 100000
required:
  - name
