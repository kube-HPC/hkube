get:
  tags:
    - Graph
  summary: Query the pipeline graph
  description: Returns the graph data by jobId, node and tasks
  parameters:
    - name: jobId
      in: path
      description: Unique identifier representing jobId
      required: true
      schema:
        type: string
    - name: node
      in: query
      description: The node name
      required: false
      schema:
        type: string
    - name: sort
      in: query
      description: Sort by status, error or any other property
      required: false
      schema:
        type: string
    - name: order
      in: query
      description: Order by asc or desc
      required: false
      schema:
        type: string
    - name: from
      in: query
      description: The start index
      required: false
      schema:
        type: integer
    - name: to
      in: query
      description: The end index
      required: false
      schema:
        type: integer
  responses:
    "200":
      $ref: "#/components/responses/graph_successGraph"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/graph_notFound"
    default:
      $ref: "#/components/responses/default"
