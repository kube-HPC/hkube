post:
  tags:
    - Pipeline Versions
  summary: Apply pipeline version
  description: Change the current pipeline version
  requestBody:
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/applyPipelineVersion"
    description: an object representing the pipeline name and the version to change to
    required: true
  responses:
    "200":
      $ref: "#/components/responses/versionsPipelines_successApply"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/versionsPipelines_notFound"
    default:
      $ref: "#/components/responses/default"
