get:
  tags:
    - Algorithm Versions
  summary: Get algorithm versions
  description: Returns list of algorithm versions.
  parameters:
    - name: name
      in: path
      description: algorithm name
      required: true
      schema:
        $ref: "#/components/schemas/algorithmName"
    - name: sort
      in: query
      description: sort by asc or desc
      schema:
        type: string
    - name: order
      in: query
      description: order by
      schema:
        type: string
    - name: limit
      in: query
      description: limit
      schema:
        type: integer
  responses:
    "200":
      $ref: "#/components/responses/versionsAlgorithms_successGetMany"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    default:
      $ref: "#/components/responses/default"
