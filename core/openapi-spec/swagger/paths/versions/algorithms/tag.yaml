post:
  tags:
    - Algorithm Versions
  summary: Tag algorithm version
  description: Tag algorithm version with pinned and tags
  requestBody:
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/algorithmVersionTag"
    description: an object representing all information needed for stored pipeline execution
    required: true
  responses:
    "201":
      $ref: "#/components/responses/versionsAlgorithms_successTag"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/versionsAlgorithms_notFound"
    default:
      $ref: "#/components/responses/default"
