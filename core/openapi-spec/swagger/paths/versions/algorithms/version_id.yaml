get:
  tags:
    - Algorithm Versions
  summary: Get version
  description: Returns algorithm version
  parameters:
    - name: name
      in: path
      description: algorithm name
      required: true
      schema:
        $ref: "#/components/schemas/algorithmName"
    - name: version
      in: path
      description: algorithm version
      required: true
      schema:
        type: string
  responses:
    "200":
      $ref: "#/components/responses/versionsAlgorithms_successGetSingle"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/versionsAlgorithms_notFound"
    default:
      $ref: "#/components/responses/default"
delete:
  tags:
    - Algorithm Versions
  summary: Delete version
  description: Delete existing algorithm version.
  parameters:
    - name: name
      in: path
      description: algorithm name
      required: true
      schema:
        $ref: "#/components/schemas/algorithmName"
    - name: version
      in: path
      description: algorithm version
      required: true
      schema:
        type: string
  responses:
    "200":
      $ref: "#/components/responses/versionsAlgorithms_successDelete"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/versionsAlgorithms_notFound"
    default:
      $ref: "#/components/responses/default"
