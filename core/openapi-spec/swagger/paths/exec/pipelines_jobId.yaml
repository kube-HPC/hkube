get:
  tags:
    - Execution
  summary: Get pipeline data
  description: Returns the executed pipeline data
  parameters:
    - name: jobId
      in: path
      description: Unique identifier representing pipeline execution
      required: true
      schema:
        type: string
  responses:
    "200":
      $ref: "#/components/responses/exec_successGetSinglePipeline"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/exec_notFound"
    default:
      $ref: "#/components/responses/default"
