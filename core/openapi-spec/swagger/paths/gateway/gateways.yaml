get:
  tags:
    - Gateway
  summary: Get gateways
  description: Get list of all gateways
  responses:
    "200":
      $ref: "#/components/responses/gateway_successMany"
    "401":
      $ref: "#/components/responses/unauthorized"
    default:
      $ref: "#/components/responses/default"
