get:
  tags:
    - Boards
  summary: Optunaboard details
  description: Optunaboard details
  parameters:
    - name: id
      in: path
      description: Unique identifier representing optunaboard
      required: true
      schema:
        type: string
  responses:
    "200":
      $ref: "#/components/responses/boards_successGetOptuna"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/boards_notFoundBoard"
    default:
      $ref: "#/components/responses/default"
delete:
  tags:
    - Boards
  summary: Stop OptunaBoard
  description: Stop OptunaBoard
  parameters:
    - name: id
      in: path
      description: Unique identifier representing OptunaBoard
      required: true
      schema:
        type: string
  responses:
    "200":
      $ref: "#/components/responses/boards_successDelete"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/boards_notFoundBoard"
    default:
      $ref: "#/components/responses/default"
