get:
  tags:
    - Resources
  summary: Get status for an unscheduled algorithm
  description: >
    This endpoint gets the status of a specific algorithm that wasn’t scheduled initially but have been scheduled eventually.
  parameters:
    - name: name
      in: path
      description: algorithm name to get his unscheduled reason
      required: true
      schema:
        type: string
  responses:
    "200":
      $ref: "#/components/responses/resources_successSingle"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/resources_notFound"
    default:
      $ref: "#/components/responses/default"