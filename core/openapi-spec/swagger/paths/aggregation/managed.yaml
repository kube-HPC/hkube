get:
  tags:
    - Managed
  summary: Get preferred
  description: Get preferred
  responses:
    "200":
      description: Get preferred
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/jobsAggregationResponse"
    "400":
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/error"
    default:
      description: Unexpected error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/error"
            
