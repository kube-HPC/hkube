get:
  tags:
    - Managed
  summary: Get managed
  description: Get managed queue grouped by pipeline name
  responses:
    "200":
      $ref: "#/components/responses/managed_successPipeline"
    "401":
      $ref: "#/components/responses/unauthorized"
    default:
      $ref: "#/components/responses/default"
            
