get:
  tags:
    - Managed
  summary: Get Managed count
  description: Get managed count
  responses:
    "200":
      description: Get managed jobs
      content:
        application/json:
          schema:
            type: integer
    "400":
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/error"
    default:
      description: Unexpected error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/error"
