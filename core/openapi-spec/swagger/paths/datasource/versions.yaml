get:
    tags:
        - DataSource
    summary: List all the versions of a given dataSource name
    parameters:
        - $ref: '#/components/parameters/nameParam'
    responses:
        '200':
            $ref: '#/components/responses/datasource_successGetVersions'
        "401":
            $ref: "#/components/responses/unauthorized"
        default:
            $ref: "#/components/responses/default"