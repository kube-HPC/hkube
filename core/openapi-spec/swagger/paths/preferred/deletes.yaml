post:
  tags:
    - Preferred
  summary: Removes from preferred
  description: Removes a job from preferred queue
  requestBody:
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/removeFromPreferredRequest"
    required: true
  responses:
    "200":
      $ref: "#/components/responses/preferred_successDelete"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    default:
      $ref: "#/components/responses/default"