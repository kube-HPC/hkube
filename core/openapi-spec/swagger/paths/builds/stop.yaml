post:
  tags:
    - Builds
  summary: Stop build
  description: stop build
  requestBody:
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/buildId"
    required: true
  responses:
    "200":
      $ref: "#/components/responses/builds_successStop"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    "404":
      $ref: "#/components/responses/builds_notFound"
    default:
      $ref: "#/components/responses/default"
