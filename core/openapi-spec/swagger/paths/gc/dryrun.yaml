post:
  tags:
    - GC
  summary: DryRun all
  description: DryRun all
  requestBody:
    description: message body
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/clean"
    required: true
  responses:
    "200":
      $ref: "#/components/responses/gc_successClean"
    "400":
      $ref: "#/components/responses/badRequest"
    "401":
      $ref: "#/components/responses/unauthorized"
    default:
      $ref: "#/components/responses/default"
