get:
  tags:
    - GC
  summary: Get all statuses
  description: Get all statuses
  responses:
    "200":
      $ref: "#/components/responses/gc_successStatus"
    "401":
      $ref: "#/components/responses/unauthorized"
    default:
      $ref: "#/components/responses/default"
