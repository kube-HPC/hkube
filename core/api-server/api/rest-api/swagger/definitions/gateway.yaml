type: object
properties:
  name:
    $ref: "#/components/schemas/gatewayName"
  description:
    type: string
    description: describe the gateway
  nodeName:
    type: string
  mem:
    type: string
    description: Memory allocated for messages in queue
    format: algorithm-memory
    default: 256Mi
    example: 512Mi
