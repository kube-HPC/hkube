const config = {};

config.transport = {
    console: true,
    logstash: false,
    file: false
};

config.extraDetails = false;
config.isDefault = true;
config.verbosityLevel = 2;

module.exports = config;
