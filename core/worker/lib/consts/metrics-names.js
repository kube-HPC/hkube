const metricsNames = {
    worker_net: 'worker_net',
    worker_failed: 'worker_failed',
    worker_succeeded: 'worker_succeeded',
    worker_started: 'worker_started',
    worker_runtime: 'worker_runtime'
};

module.exports = metricsNames;
